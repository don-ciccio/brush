build/
screenshot.png
.DS_Store
*.o
*.d
compile_commands.json
external/joltc/build/
imgui.ini
editor_layout.ini

# Per-project import cache (docs/asset-pipeline.md)
.brush/

# The engine repo is not a user project (engine-dev mode runs the gym in place)
/project.def

# Packager output (run build/packager in a project)
game.pak
